language: node_js
node_js:
    - "5.0"
before_install:
    - sh init.sh
    - mkdir private
    - openssl aes-256-cbc -K $encrypted_aacaf7b8ffaf_key -iv $encrypted_aacaf7b8ffaf_iv -in config.json.enc -out private/config.json -d
script:
    - npm run lint
    - npm test

